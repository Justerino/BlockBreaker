# class to store the row and columns value in an object
class Position:
    # initializes with x and y arguments and stores them
    def __init__(self, row, column):
        self.row = row
        self.column = column
